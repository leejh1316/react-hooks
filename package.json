{
  "name": "react-hooks",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm run build:packages && pnpm --filter demo build",
    "preview": "pnpm run build:packages && pnpm --filter demo preview",
    "build:packages": "pnpm --filter \"./packages/*\" build"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
